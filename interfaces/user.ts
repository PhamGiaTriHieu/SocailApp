export interface User {
  // Define user properties here (e.g., id, name, etc.)
  id?: string;
  name?: string;
  email?: string;
  image?: string | null;
  bio?: string | null;
  address?: string | null;
  phoneNumber?: string | null;
  create_at?: string;
}
